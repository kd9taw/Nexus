[package]
publish = false
name = "q65"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Safe Rust wrapper over libtempo's native Q65 decoder (WSJT-X Q65-30A, DECODE ONLY)"

[dependencies]
tempo-fast-sys = { path = "../tempo-fast-sys" }
