[package]
publish = false
name = "msk144"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Safe Rust wrapper over libtempo's native MSK144 meteor-scatter codec (decode + TX encode)"

[dependencies]
tempo-fast-sys = { path = "../tempo-fast-sys" }
