[package]
publish = false
name = "wspr"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Safe Rust wrapper over libtempo's native WSPR decoder (DECODE ONLY)"

[dependencies]
tempo-fast-sys = { path = "../tempo-fast-sys" }
