[package]
publish = false
name = "tempo-fast"
version = "0.2.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Safe Rust wrapper over libtempo — the FT1 4-CPM turbo weak-signal modem"

[dependencies]
tempo-fast-sys = { path = "../tempo-fast-sys" }
